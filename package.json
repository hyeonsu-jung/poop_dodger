{
  "name": "poop-dodger",
  "version": "1.0.0",
  "private": true,
  "description": "Poop Survivor — Capacitor shell loading the Vercel deployment",
  "scripts": {
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "android:build": "cd android && gradlew.bat assembleDebug",
    "android:build:release": "cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
